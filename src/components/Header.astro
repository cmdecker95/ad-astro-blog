---
const links = [
  { text: "👨🏽‍🚀 Home", href: "/" },
  { text: "📚 Blog", href: "/blog" },
];
---

<header
  class="flex px-24 py-12 max-sm:px-5 max-sm:py-10 max-w-7xl w-full mx-auto"
>
  <ul class="flex gap-x-9">
    {
      links.map((link) => (
        <li>
          <a
            href={link.href}
            class="uppercase text-xl text-stone-500 hover:text-stone-700"
          >
            {link.text}
          </a>
        </li>
      ))
    }
  </ul>
</header>
